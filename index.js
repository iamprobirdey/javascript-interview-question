// what are the possible ways to create a object.

// 1. Object Contructor:
    var object = new Object();

// 2. Object create method:
    var object = Object.create(null);

// 3. Object Literal syntax:
    var object = {
        firstName: 'Probir',
        lastName: 'Dey'
    }

// 4. Function Contructor
    function Person(name){
        this.name = name,
        this.age = '31'
    }

// 5. 

